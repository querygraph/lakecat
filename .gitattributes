# Patch bridge files preserve mail-patch context markers that can look like
# trailing whitespace to `git diff --check`. The dependency contract verifies
# these files against local Sail commits with stable `git patch-id` evidence.
ci/sail-patches/*.patch -whitespace
