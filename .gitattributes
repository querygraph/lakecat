# LakeCat sources Sail from the querygraph/sail `lakecat` branch as a Cargo git
# dependency (see Cargo.toml). The former ci/sail-patches bridge — and its
# `-whitespace` exemption — was retired with that move.
